# ch174 Lint 修复协议

> **本文件是 docs/TODO.md 任务 5 的产物**：ch174 是机器 Lint 扫描报告中唯一的 ERROR 章节（违反 §3 规则——「屋里安静/院里静/夜很静/心里咚」机械排比）。

---

## 一、问题诊断

ch174 是机器 Lint 扫描报告中唯一的 ERROR 章节。**当前状态**：仓库中 ch174 尚未落盘（属于 ch11-505 残缺口）。

**机器 Lint 命中的违规模式**：
- ❌ **「屋里安静」**——抽象景物描写，违背 §3"具体景物或人物动态"规则
- ❌ **「院里静」**——同
- ❌ **「夜很静」**——同
- ❌ **「心里咚」**——机械的"心理+象声"模板，违背 §3"具体景物或人物动态"规则

**病根**：subagent 把"静"和"咚"当万能填充词，机械排比堆叠。

---

## 二、修复策略（5 条）

### 策略 1：把"屋里安静"换成具体景物
```
❌ 反例：屋里安静。
✅ 正例：屋里那张旧桌上，ch162 林夙喝剩的药碗还在，碗里那点药已经凉透了。
```

### 策略 2：把"院里静"换成具体动态
```
❌ 反例：院里静。
✅ 正例：院里槐树的叶子落了一片——落在水缸边——缸里的水不动——叶子也不动。
```

### 策略 3：把"夜很静"换成具体声/光
```
❌ 反例：夜很静。
✅ 正例：夜里窗外的蛩声断了两下——远处有人在敲更——第三下又接上了。
```

### 策略 4：把"心里咚"换成具体动作或触感
```
❌ 反例：心里咚了一下。
✅ 正例：他指节在袖里按了一下——袖口的线压着他腕骨——他知道是她回来了。
```

### 策略 5：替换"心里咚/跳/颤"模板
| 反例 | 正例 |
|---|---|
| 心里咚了一下 | 他指节在袖里按了一下 |
| 心里颤了一下 | 他把那两个字看了三遍才放下来 |
| 心里跳了一下 | 他把袖口往里翻了一寸 |
| 心里压了一口气 | 他停了三个呼吸才开口 |

---

## 三、Lint 守门（新增 QC-N）

需要在 `prompts/rewrite-orchestrator.txt` 增加：

```
QC-N ch174 Lint 守门（§任务 5 · docs/TODO.md）:
❌ 不得出现"屋里安静/院里静/夜很静/心里咚/心里跳/心里颤/心里压了一口气"
❌ 不得出现"X 静"+ 抽象景物堆叠（≥3 次）→ 必须替换为具体景物或人物动态
✅ 必须每章有 ≥ 1 处具体景物描写（触觉/视觉/听觉中至少一种）
```

---

## 四、当前状态

> **关键事实**：ch174 在仓库中**尚未落盘**（属于 ch11-505 残缺口）。本协议为未来补写该章时使用。

**当 ch174 补写时**：
1. 必须先读本协议
2. 不得出现上述 5 类反例
3. 用 5 条策略中的正例替换
4. 写完后由 orchestrator 按 QC-N 门验收

---

## 五、配套修复样板（≤ 30 字正例）

```yaml
# 反例 vs 正例速查（写手自检表）
反例: 屋里安静
正例: 桌上那只 ch162 剩的药碗凉了

反例: 院里静
正例: 院里一片槐叶落在水缸边

反例: 夜很静
正例: 窗外蛩声断了两下又接上

反例: 心里咚
正例: 他指节在袖里按了一下

反例: 心里颤了一下
正例: 他把那两个字看了三遍才放
```

---

## 六、出处

本协议由 docs/TODO.md 任务 5 调度生成。配套：
- **任务 5.1 优化 ch174**：⏸ ch174 未落盘，待补写时套用本协议
- **任务 5.2 orchestrator QC-N 验收门**：⏸ 待补 QC-N 门